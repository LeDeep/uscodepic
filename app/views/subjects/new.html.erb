<% provide :title, "Add a new subject!" %>

<%= render :partial => 'layouts/errors', :locals => {:object => @subject} %>

<%= form_for @subject do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, :autofocus => true %>

  <%= f.submit "Add", :class => 'btn btn-primary' %>
<% end %>